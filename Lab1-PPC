import java.io.BufferedWriter;
import java.io.OutputStreamWriter;
import java.io.IOException;
import java.lang.NumberFormatException;

public class Lab1-PPC {
    public static int getFactorial(int f) {
        int result = 1;
        for (int i = 1; i <= f; i++) {
           result = result * i;
        }
        return result;
    }
    public static int function(double var_x, double var_n){
        int y; 
        y = Math.pow(-1, var_n)*Math.pow(var_x, (2*var_n+1));
        y = y/getFactorial(2*var_n+1);
        return y;
    }
    public static void main(String args[]) {
        double x = Double.parseDouble(args[0]);
        double h = Double.parseDouble(args[1]);
        double left = Math.sin(x);
        double current = function(x,0);
        double right = 0;
        double previous = current;
        double e = current - previous;
        for(int n = 1; e > h; n++){
           right+=current;
           previous = current;
           current = function(x,n);
        }
        if(left==right) System.out.println(left + " = " + right);
        else System.out.println(left + " Not equal " + right);
    }
}
